# 산들마을 회의록 — AI 작업 지침

이 저장소를 고치는 AI/작업자는 먼저 아래를 읽는다.

1. `README.md`
2. `docs/DATA.md` — 레코드 구조·적재 절차·저장 시 주의사항
3. `docs/PLAN.md` — 계획·원칙
4. `CHANGELOG.md` 최근 항목 — 지금 어디까지 와 있는지

포털·Archive 저장소(`SandleAPT/sandleapt.github.io`)와 함께 작업할 때는 그쪽 `AGENTS.md`와 `docs/AI_WORKFLOW.md`도 따른다. 사용자 결정 기록은 그쪽 `docs/WORK_STATUS.md`에 모은다.

## 작게 잘라서 한다 (사용자 지시, 2026-09-01)

사용자 요청: **"작게 잘라서 일을 했으면 해."**

한 번에 여러 기능을 묶어 오래 작업하면 사용자는 그동안 무슨 일이 벌어지는지 볼 수 없고, 방향을 바로잡을 틈도 없다. 잘못된 판단이 있어도 다 끝난 뒤에야 드러난다.

1. **한 번에 하나의 기능 묶음만.** 끝내고 검증하고 커밋하고 보고한 다음에 넘어간다.
2. **묶음 크기**: 커밋 하나로 설명되고, 검증 한 번으로 확인되고, 두세 문단으로 보고할 수 있는 정도.
3. **오래 기다려야 하면 먼저 말한다.** 배포 반영 1~2분, 백업 워크플로 10~15분. 기다리는 동안 다른 파일을 고치지 않는다 — 그렇게 하다 백업 워크플로의 push가 거부돼 실패했다(2026-09-01 run #29).
4. **탐색과 수정을 섞지 않는다.** 원인을 찾는 중이면 찾는 것만 한다.

예외: 사용자가 "한 번에 다 해줘"라고 하거나, 쪼개면 중간 상태가 깨지는 경우(캐시 키와 파일을 함께 올려야 하는 배포 등). 그때도 왜 묶었는지 한 줄 적는다.

## 이 저장소에서 특히 조심할 것

`docs/DATA.md`에 자세히 있고, 사고가 났던 것들만 추린다.

- **배포 전 문법 검사.** 이 PC에는 node가 없다. 배포본을 받아 같은 수정을 적용한 뒤 브라우저 파서(`new Function`)로 통과를 확인하고 올린다. v49에서 문법 오류를 그대로 배포한 적이 있다.
- **레코드를 다시 저장할 때 `date`를 반드시 실어 보낼 것.** 빼거나 `""`로 보내면 기존 날짜가 지워진다. 2026-09-01에 이 실수로 64건의 날짜가 지워졌다.
- **재조회 검증에 `date`도 포함할 것.** 위 사고는 "저장·재조회 검증 OK"를 통과했다. `json`만 대조했기 때문이다. 시트가 날짜를 Date 값으로 바꾸므로 문자열이 아니라 같은 날인지로 비교한다.
- **버전을 올릴 때 캐시 키를 함께 갱신할 것.** `index.html`의 `?v=NN` 전부 + `<title>` + `appVersionInfo`. 하나라도 빠지면 방문자가 옛 파일을 받는다.
- **한글 파일을 PowerShell 텍스트 파이프라인으로 고치지 말 것.** `Get-Content -Raw` → `Set-Content`는 한글을 깨뜨린다. 편집 도구나 `[IO.File]::ReadAllText/WriteAllText`(인코딩 명시)를 쓴다.
- **적재는 `SandleImporter.run(JOB)`으로.** 저장·재조회·목록 대조 검증이 함께 돌고 실패하면 throw한다. 검증 없이 "적재 완료"로 적지 않는다.
